## Quick Start

This project is fully containerized. You only need Docker installed. 

Run this single command to automatically install dependencies, run migrations, and start the app:

```bash
docker compose up -d --build
```

## Stop the App

```bash
docker compose down
```
